version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb_opala
    restart: always
    ports:
      - "27018:27017"
    env_file:
      - ./.env.local
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
